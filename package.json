{
  "name": "reminderapp",
  "version": "1.0.0",
  "description": "Toilet Cleaned Reminder",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "cd client && npm install",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start"
  },
  "author": "Roma Fominih",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.6",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "helmet": "^4.4.1",
    "joi": "^17.4.0",
    "mongodb": "^3.6.5",
    "mongoose": "^5.12.2",
    "node-cron": "^3.0.0"
  }
}
